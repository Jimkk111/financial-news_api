<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.news.mapper.AiMessageMapper">

    <!-- 批量查询每个会话的最后一条消息（窗口函数，消除 listSessions 的 N+1 查询） -->
    <select id="selectLastMessages" resultType="com.financial.news.entity.AiMessage">
        SELECT t.* FROM (
            SELECT m.*, ROW_NUMBER() OVER (PARTITION BY m.session_id ORDER BY m.created_at DESC) AS rn
            FROM ai_messages m WHERE m.session_id IN
            <foreach collection="sessionIds" item="id" open="(" separator="," close=")">#{id}</foreach>
        ) t WHERE t.rn = 1
    </select>

    <resultMap id="BaseResultMap" type="com.financial.news.entity.AiMessage">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_messages (session_id, role, content, created_at)
        VALUES (#{sessionId}, #{role}, #{content}, #{createdAt})
    </insert>

    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT id, session_id, role, content, created_at
        FROM ai_messages
        WHERE session_id = #{sessionId}
        ORDER BY created_at ASC
    </select>

    <delete id="deleteBySessionId">
        DELETE FROM ai_messages WHERE session_id = #{sessionId}
    </delete>

</mapper>
